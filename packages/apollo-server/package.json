{
    "name": "bun-todo-server",
    "module": "index.ts",
    "type": "module",
    "scripts": {
        "predev": "bun run gen",
        "dev": "bun run --hot index.ts",
        "gen": "graphql-codegen"
    },
    "devDependencies": {
        "@graphql-codegen/typescript-resolvers": "^4.0.1"
    },
    "dependencies": {
        "@apollo/server": "^4.9.5"
    }
}
